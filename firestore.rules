rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Admin paneli okuma yetkisi (şimdilik geliştirme için açık)
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
